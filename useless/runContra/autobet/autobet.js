const { listenArrayEvents } = require("chart.js/helpers")
const { TaskManager } = require("puppeteer")

let betEventDuo = bigPendingBetList[1]

let isAutoBetting = [arr of acc]
let isCoolingDown = []
{
  acc1
  acc2
  for each acc {
    homeName
  awayName
  leagueName
  malayOdds
  buttonID
  scrapedTime
  brainTime
  VIG
  }
difference
  expectedStake //if higher vig, can enter more???? only 3838 or both?
}



const autobet(betEventDuo){

  //reaad params

  //read can bet or no bet
  acc1canBet = false, acc2canBet = true

  synchronously do two tasks

  1. ticket acc1
  2. ticket acc2

  Promise.all(these two)
  else 'throw' err

  check for arb condition again 
  if ok 
  check priority list 

  bet according to priority list

  
  save to success bet list
  betEventDuo +
    ticketOdds, bettedOdds, tickettedTime, bettedTime, stake, bettedMalayDifference + minMax stake'

  start cooldown for these two account(brain should also include isAutoBetting and isCoolingDown, if is autobetting then dont need to brain)






}
